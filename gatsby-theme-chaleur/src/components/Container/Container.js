import React from 'react';

export default ({ children }) => (
    <div style={{
        margin: '1rem auto',
        padding: 'auto',
        maxWidth: '80%',
        width: '600px'
    }}>
        {children}
    </div>
)