import React from 'react';

export default ({ children }) => (
    <div style={{
        padding: '0',
        margin: '0',
        fontFamily: 'Verdana, Geneva, sans-serif',
    }}>
        {children}
    </div>
)