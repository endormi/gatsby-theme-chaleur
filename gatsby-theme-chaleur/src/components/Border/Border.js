import React from 'react';

export default ({ children }) => (
    <div style={{
        padding: '15px',
        margin: '8px 0 4px 0',
        border: '1px solid black',
        textAlign: 'center',
    }}>
        {children}
    </div>
)