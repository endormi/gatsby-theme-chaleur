import React from 'react';

export default ({ children }) => (
    <div style={{
        padding: '20px',
        margin: '12px 0 10px 0',
        border: '1px solid black',
    }}>
        {children}
    </div>
)