import React from "react";

export default ({ children }) => (
  <Main style={{
    fontFamily: 'Roboto, sans-serif, serif',
    margin: '0 auto',
    display: 'block',
  }}>
    { children }
  </Main>
)